export const Options = () => {
    return (
        <section className="options">
            <div className="options-title">Build & manage distributed teams like no one else.</div>
        </section>)
}